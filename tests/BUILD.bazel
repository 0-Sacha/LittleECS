load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "units",
    srcs = glob([ "**/*.hxx", "**/*.cxx" ], exclude=["Perf/**"]),
    includes = [ "tests/" ],
    copts = select({
        "@rules_cc//cc/compiler:msvc-cl": ["/std:c++20"],
        "//conditions:default": ["-std=c++20"],
    }),
    deps = [ "@streamformat//:StreamFormat", "//:lecs" ],
    visibility = ["//visibility:public"],
)

cc_test(
    name = "perf",
    srcs = glob([ "**/*.hxx", "**/*.cxx" ]),
    includes = [ "tests/" ],
    copts = select({
        "@rules_cc//cc/compiler:msvc-cl": ["/std:c++20"],
        "//conditions:default": ["-std=c++20"],
    }),
    deps = [ "@streamformat//:StreamFormat", "//:lecs" ],
    visibility = ["//visibility:public"],
)
